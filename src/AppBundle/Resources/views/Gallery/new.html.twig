{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Gallery creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('gallery') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
