{% extends 'AppBundle:default:layout_admin.html.twig' %}
{% block admin_content -%}
    <h1>Image edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('image') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
